foo.a.x(5,6);
const x = await get();
var p = new Point(1,2);
var fn = function() { x = 5; return 45 };
foo.a().x(5,6);
const fn = (a,b) => a+b;
const fn = (a,b) => { x = a+b; }
const fn = () => { x = a+b; }
const fn = (a) => (b,c,j) => { x = a+b; }
